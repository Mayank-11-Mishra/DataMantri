# SQLite Configuration (No PostgreSQL/Redis needed)

# Database - SQLite
DATABASE_URL=sqlite:///./datamantri_pipelines.db

# JWT Settings
SECRET_KEY=local-dev-secret-key-change-in-production-12345
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Redis (optional, using memory for dev)
REDIS_URL=redis://localhost:6379/0

# Celery - Memory mode (no Redis needed)
CELERY_BROKER_URL=memory://
CELERY_RESULT_BACKEND=db+sqlite:///./celery_results.db

# Google Cloud (Optional)
GOOGLE_APPLICATION_CREDENTIALS=
GCP_PROJECT_ID=
